Ctrl +d  快速复制当前行到下一行

死循环：一个很大的BUG,绝对不能出现的。重复一直的去执行
增量：主要是为了解决死循环的

break作用：终止循环继续运行。配合判断使用
continue作用：跳过这一步循环，直接进入到下一步循环

外循环执行一次，内循环会执行多次

一个print()是占一行的
end的作用，可以改变print结束的换行问题

\t 横向制表符，能够让文本垂直对齐

看代码的运行流程，可以打断点，debug

列表的作用：保存大量的数据
若把pop放在程序中，返回被删除数据的值

函数：直接一个单词带着小括号就能用的  例如：len(列表)
方法：必须前面有个对象，才能够使用的  例如：列表.append(数据)
关键字：							   例如：del[索引]=数据

for 变量 in 集合:

    循环体代码
else:
    没有通过 break 退出循环，循环结束后，会执行的代码

字符串的切片的格式：
字符串[开始索引:结束索引:步长]

if 字符串[-1] not in "男女"：判断字符串最后一个是不是男女

写一个登陆系统包含很多功能时：可以利用while True然后用continue及break组合使用

" " in 字符串：用于判断字符串中是否有空格

pass是空语句，不做任何事情，用作占位语句